# PyTorch (install separately with CUDA support)
# For Arch Linux + CUDA 12.x:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

torch>=2.0.0,<2.6  # PyTorch 2.6+ has weights_only=True default (incompatible with RecBole)
torchvision>=0.15.0,<0.21  # Match torch 2.5.x compatibility
torchaudio>=2.0.0,<2.6

# RecBole framework (includes LightGCN, SimpleX, EASE, ItemKNN)
recbole>=1.1.1

# RecBole dependencies (required for some models)
kmeans-pytorch>=0.3
hyperopt>=0.2.7

# Ray Tune (Python 3.10 호환, verbose 버그 해결 버전)
ray[tune]==2.20.0
pydantic>=2.0.0,<3.0.0
optuna>=3.0.0
pyarrow>=14.0.0,<15.0.0  # Ray 2.20.0은 PyArrow 14.x와 호환
aiohttp>=3.8.0
aiosignal>=1.3.0

# Core scientific libraries (RecBole 호환 버전)
numpy>=1.24.0,<2.0    # RecBole는 NumPy 2.0 미지원
pandas>=2.0.0
scipy>=1.10.0,<1.13.0 # RecBole는 SciPy 1.13+ 미지원 (dok_matrix._update 제거됨)
scikit-learn>=1.3.0

# Progress bar
tqdm>=4.65.0

# Jupyter notebook support
jupyter>=1.0.0
ipykernel>=6.25.0
notebook>=7.0.0

# Optional: visualization
matplotlib>=3.7.0
seaborn>=0.12.0
